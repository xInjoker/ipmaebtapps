
// import mysql from 'mysql2/promise';
// import type { Tender } from '@/lib/tenders';

// // Assume connection pool is created and exported from a central db file
// // import { pool } from './database';

// /**
//  * Fetches all tenders from the database.
//  */
// /*
// export async function getTenders(): Promise<Tender[]> {
//   let connection;
//   try {
//     connection = await pool.getConnection();
//     const [rows] = await connection.execute('SELECT * FROM tenders');
//     return rows as Tender[];
//   } catch (error) {
//     console.error('Failed to fetch tenders:', error);
//     return [];
//   } finally {
//     if (connection) connection.release();
//   }
// }
// */
